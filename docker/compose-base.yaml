# -- Eva Intelligence & Logic (The Base Hub) --
name: eva-base

networks:
  eva-net:
    external: true

services:
  # -- GATEWAY: API SECURE PROXY --
  api-gateway:
    image: nginx:alpine
    container_name: eva-api-gate
    networks:
      - eva-net
    env_file:
      - /volume1/ros/secrets/eva_central.env
    volumes:
      - ../config/nginx.conf.template:/etc/nginx/templates/default.conf.template:ro
    restart: always

  # -- CORE: EVA (Brain & Logic) --
  base:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile.base
      args:
        ROS_DISTRO: ${ROS_DISTRO:-humble}
    container_name: eva-base
    networks:
      - eva-net
    env_file:
      - ../.env
    environment:
      - DISPLAY=$DISPLAY
      - QT_X11_NO_MITSHM=1
      - MASTER_SEARXNG_LANGUAGE=en-US
      - BOB_LAUNCH_AUTOABORT=0
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: ros2 launch bob_launch generic.launch.py config:=/ros2_ws/src/bob_central/launch/eva_base_launch.yaml
    dns: ["192.168.32.100"]
    volumes:
      - ../:/ros2_ws/src/bob_central # Map source for self-evolution
      - /volume1/ros/eva:/root/eva # Unified persistent home
      - /volume1/ros/eva/ssh:/root/.ssh # Persistent AI Identity (Keys)
      - /tmp/eva:/tmp
    restart: on-failure

  # -- STATUS: EVA (Telemetry & Dashboard Status Launcher) --
  status:
    image: eva-base:latest
    container_name: eva-status
    depends_on:
      - base
    networks:
      - eva-net
    env_file:
      - ../.env
    command: ros2 launch bob_launch generic.launch.py config:=/ros2_ws/src/bob_central/launch/status_launch.yaml
    dns: ["192.168.32.100"]
    volumes:
      - ../:/ros2_ws/src/bob_central
      - /tmp/eva:/tmp
    restart: always
